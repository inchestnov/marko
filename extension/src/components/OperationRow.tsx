// A single operation row: icon + path + before/after. See
// docs/architecture.md §10.2.
// TODO(extension-agent): implement real rendering.

export default function OperationRow() {
  return <div>OperationRow placeholder</div>;
}
